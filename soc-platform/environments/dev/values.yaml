# environments/dev/values.yaml

# This file contains overrides for a dev environment.
#
# NOTE: Per-chart overrides have been moved to separate files:
#   - environments/dev/thehive.yaml
#   - environments/dev/cortex.yaml
#   - environments/dev/webhook-adapter.yaml
#
# Cassandra configuration is in environments/dev/cassandra.yaml
