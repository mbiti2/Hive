# environments/lab/values.yaml

# This file contains overrides for a lab/development environment.
# It uses smaller resource limits and sizes appropriate for local clusters like k3s.
#
# NOTE: Per-chart overrides have been moved to separate files:
#   - environments/lab/thehive.yaml
#   - environments/lab/cortex.yaml
#   - environments/lab/webhook-adapter.yaml
#
# Cassandra configuration is in environments/lab/cassandra.yaml
# This file is kept for chart-specific values that could be shared
# across charts (if any in the future).
