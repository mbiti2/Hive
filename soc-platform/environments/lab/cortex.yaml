# environments/lab/cortex.yaml
# Overrides for the Cortex chart in lab/development environment.
# Values are at top level to match the chart's values.yaml structure.

replicaCount: 1

resources:
  requests:
    cpu: "250m"
    memory: "512Mi"
  limits:
    cpu: "500m"
    memory: "1Gi"

elasticsearch:
  # We use the existing Wazuh indexer
  host: wazuh-wazuh-helm-indexer-api.wazuh.svc.cluster.local
  port: 9200
  ssl: true
  username: "admin"
  password: "admin"

search:
  acceptAnyCertificate: true

ingress:
  enabled: true
  className: "traefik"
  hosts:
    - host: cortex.lab.local
      paths:
        - path: /
          pathType: ImplementationSpecific
